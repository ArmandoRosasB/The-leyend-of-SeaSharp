using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "NewButton", menuName = "ButtonFlag")]

public class MainController : ScriptableObject
{
    // Start is called before the first frame update
    public bool Active2;

    public bool Active3;

}
